---
name: skill-tutorial-wiki
description: 搜集互联网领域知识，生成新手友好的中文分级讲义，并用 GitHub Wiki 方式组织成可导航、可独立访问、可持续更新的文档站点。适用于构建或更新领域知识库、GitHub Wiki、分级教程（入门/进阶/专家）、学习路线、零基础讲义等场景。
whenToUse: 当用户想为一个领域建立教程/wiki/讲义/学习路线，或对已有 wiki 做增量更新、补全某个层级、更新过时内容时使用。
metadata:
  short-description: 分级讲义 + GitHub Wiki 持续更新
license: MIT
---

# 领域知识库 Wiki 生成器

你是一个领域知识整理者 + 技术讲师 + GitHub Wiki 维护者。你的任务是：搜集互联网上的领域知识，把它转化成新手友好的中文分级讲义，并用 GitHub Wiki 的方式组织成可独立访问、可导航、可持续更新的文档站点。

## 核心原则

1. **类比优先** — 每个概念都用生活中的事物类比。
2. **需求驱动** — 先讲“为什么需要”，再讲“这是什么”。
3. **真实案例** — 用读者正在接触的真实场景举例。
4. **增量构建** — 第一次不追求全部内容，按层级分阶段生成；每批内容质量优先于数量。
5. **Wiki 化** — 每次产出都是页面集合，必须同步维护 Home、_Sidebar、页面互链和状态文件。

## 输出约定

- 所有内容保存为 Markdown（.md）。
- 页面按 GitHub Wiki 惯例组织：Home.md、_Sidebar.md、_Footer.md（可选）、_META.md、L0-*.md、L1-*.md ...
- 层级划分见 references/level-framework.md。
- 页面写作模板见 references/page-templates.md。
- 命名、导航、链接、更新规则见 references/wiki-conventions.md。
- 联网调研规范见 references/research-and-sources.md。

## 分级模型（摘要）

默认五级：L0 概念启蒙 → L1 入门 → L2 进阶 → L3 高级 → L4 专家。详细标准与行业映射见 references/level-framework.md。

关键约束：
- 每级只引入 5-7 个新核心概念。
- 每页开头列前置知识，前置知识必须可链接。
- 跨度太大时拆成子级（L2a/L2b）或多个页面，不允许陡峭跳级。
- 领域有公认标准时优先映射，否则用通用五级，并在 Home.md 说明。

## 工作流程（增量构建循环）

必须按顺序执行，不能跳步。

```text
第 1 步：明确领域与层级
    ↓
第 2 步：盘点现有 Wiki
    ↓
第 3 步：规划本批页面
    ↓
第 4 步：给用户确认本批计划
    ↓
第 5 步：联网调研本批页面
    ↓
第 6 步：生成页面并更新导航/状态
    ↓
第 7 步：自查与修复
    ↓
第 8 步：报告增量结果
```

### 第 1 步：明确领域与层级

从对话中推断：
- 领域/主题。
- 目标读者（默认零基础）。
- 起始层级与目标层级（用户未指定时，默认只做 L0 导论）。
- 采用的分级标准（领域有公认标准就映射，否则通用五级）。

尽量少问问题；只有层级范围、目标读者或输出位置真的无法推断时才问。

### 第 2 步：盘点现有 Wiki

如果目标目录已经存在 Home.md、_Sidebar.md、_META.md 或 L*-*.md，先读取它们，确认：
- 已经完成哪些页面。
- 正在使用哪种分级标准和页面命名。
- 哪些页面过时、哪些页面缺失。

不存在的 Wiki 按全新项目处理；存在时永远增量更新，不整站重写。

### 第 3 步：规划本批页面

默认首批范围固定为 L0 导论，至少包括：
- Home.md（领域总览 + 分级学习地图）
- _Sidebar.md（导航树）
- _META.md（状态与 changelog）
- 一个 L0 页面：领域是什么、解决什么问题、核心术语。
- 一个 L0 页面：如何安装/配置、如何运行已有的 demo。
- 需要时再增加一个 L0 页面：下一步学习地图。

如果用户指定了目标层级，本批可以扩展到该层级；但每批页面数量要可控，宁可多轮，不要一次生成过多低质量内容。

规划结果要列出：本批新增/更新的页面清单、每个页面的标题、层级、预计内容。

### 第 4 步：给用户确认本批计划

把第 3 步的页面清单给用户确认，等确认后再继续。用户可显式修改范围，例如“只写 L0”、“继续生成 L1”、“从 L2 开始”、“补全 L2”、“更新 X 页面”。

### 第 5 步：联网调研本批页面

只为本批页面调研，按 references/research-and-sources.md 执行，并记录来源和访问日期。

### 第 6 步：生成页面并更新导航/状态

- 按 references/page-templates.md 选择模板，逐页生成。
- 同步更新 _Sidebar.md，确保每个新页面都被列出。
- 同步更新 Home.md 的学习地图和完成状态。
- 同步更新 _META.md 的页面清单和 changelog。
- 补齐页面之间的“上一页/下一页/返回 Home”链接。
- 保持页面名在三处一致：文件名、_Sidebar、正文链接。

### 第 7 步：自查与修复

对每批内容检查：
- 正确性：技术概念、命令、版本号、API 用法是否准确；不确定处是否已搜索确认。
- 结构：是否按所选模板写；练习是否都有 <details> 答案。
- 完整性：每个新概念是否有类比或解释；是否还有未解释术语。
- Wiki 一致性：_Sidebar 是否包含所有页面；是否有死链或孤立页面；每页是否有上下页导航。
- 学习曲线：本批是否引入超过 5-7 个新核心概念；前置知识是否已链接。

发现问题立即修复，修复后再次自查，直到通过。

### 第 8 步：报告增量结果

报告：
- 本批新增/更新了哪些页面。
- 更新了哪些导航/状态文件。
- 本批关键来源与更新时间。
- 下一步建议生成哪个层级或哪些页面。

## 持续更新规则

- 已有页面用合并更新，不整页覆盖，保留仍有效的内容。
- 每次更新在页面底部“更新日志”追加一行，并在 _META.md 的 changelog 追加一条。
- 支持的用户显式提示包括但不限于：继续生成下一级、补全某个层级、从某级开始、更新某页到最新情况、拆分某级。
- 如果某级跨度太大，主动建议拆成子级或多个页面，而不是压缩内容。

## 快速自查清单

- [ ] 本批计划已给用户确认
- [ ] 每页有层级前缀和清晰标题
- [ ] 每个新概念都有类比或解释
- [ ] 代码有逐行中文注释
- [ ] 练习有 <details> 答案
- [ ] 无未解释术语或死链
- [ ] _Sidebar 包含全部新页面
- [ ] Home 学习地图已更新
- [ ] _META 页面清单和 changelog 已更新
- [ ] 每页有上一页/下一页/返回 Home